<title>Edit : <%= @product.title %></title>
<h1>Editing <%= @product.title %></h1>
<br>
<%= render 'form', product: @product %>

<br>

<div class="col btn btn-sm btn-info"><%= link_to 'Show', @product %></div> 
<div class="col btn btn-sm btn-primary"><%= link_to 'Back', products_path %></div>
